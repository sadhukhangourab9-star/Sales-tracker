services:
  - type: web
    name: sales-tracker
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -b 0.0.0.0:10000 app:app"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0